package com.example.mobileapp

import io.flutter.embedding.android.FlutterActivity

// PUBLIC_INTERFACE
/**
 * Android entry point activity for the Flutter application using v2 embedding.
 *
 * Package must match the AndroidManifest and Gradle namespace:
 * - package: com.example.mobileapp
 * - namespace: com.example.mobileapp
 *
 * FlutterActivity is provided by the Flutter embedding artifacts resolved via
 * the dev.flutter.flutter-gradle-plugin and Flutter tools included in settings.gradle.
 */
class MainActivity : FlutterActivity()
